{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "cronSchedule": "0 10 * * *",
    "startCommand": "python manage.py ingest_form_d --days 3 && python manage.py ingest_form_c --days 3 && python manage.py ingest_form_1a --days 3",
    "restartPolicyType": "NEVER"
  }
}
